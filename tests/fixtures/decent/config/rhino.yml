# Decent project — most assertions pass
project:
  stage: early

value:
  hypothesis: "Developers ship features with confidence using automated checks"
  user: "Small dev teams"
  signals:
    - name: deploy_confidence
      description: "Deploys without manual QA"
      target: ">90% deploys are green"
      measurable: true

scoring:
  cache_ttl: 300
  health_gate_threshold: 20
  health_warn_threshold: 40
  onboarding_cap: 50
