# Healthy project — everything passing
project:
  stage: growth

value:
  hypothesis: "Teams coordinate work without meetings using async status updates"
  user: "Remote engineering teams"
  signals:
    - name: meeting_reduction
      description: "50% fewer status meetings"
      target: "Meetings < 3/week"
      measurable: true

scoring:
  cache_ttl: 300
  health_gate_threshold: 20
  health_warn_threshold: 40
  onboarding_cap: 50
