---
name: init
description: Legacy alias for /setup. Onboards a project into rhino-os.
user-invocable: true
---

# Init — Legacy Alias

This command is an alias for `/setup`. Run `/setup` instead.

When invoked, execute the setup skill: read and follow the instructions in the setup skill (`~/.claude/commands/setup/SKILL.md`).
